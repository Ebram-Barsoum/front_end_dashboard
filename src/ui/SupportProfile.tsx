/* eslint-disable no-empty-pattern */
interface SupportProfileProps {
    fisrtName: string;
    lastName: string;
    type: string;
    email: string;
    phone: string;
    city: string;
    createdAt: Date | string;

}

export default function SupportProfile({ }: SupportProfileProps) {
    return (
        <div>

        </div>
    )
}
